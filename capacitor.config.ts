import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'luncoffee.ionic',
  appName: 'lun_coffee',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
